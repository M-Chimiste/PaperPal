# Core ML & Deep Learning
torch
torchvision
torchaudio
# Transformers 5.9+ regressed RT-DETR-v2 on Apple MPS by allocating float64
# tensors on-device. Docling's layout model uses this path, so retain the last
# MPS-compatible implementation until the upstream fix is released.
transformers==5.8.1
# numpy capped at 2.2.x: numba (via umap-learn, star map) rejects numpy>=2.3.
# scipy>=1.16 needed on macOS 27+ (older wheels fail dyld __thread_bss checks).
numpy>=2.0,<2.3
scipy>=1.16

# NLP & Text Processing
spacy
spacy-layout
docling
markitdown
markdown
mdpd
tiktoken
unidecode

# Embeddings & Vector Search
sentence-transformers

# Scheduler
apscheduler>=3.10.4

# Research Agent Dependencies
arxiv>=1.4.7
simhash>=2.1.0
langgraph
yake

# LLM & AI Services
# LLMFactory - Unified inference interface for multiple providers
# v0.2.4 includes LM Studio singleton handling fix
llmfactory @ git+https://github.com/M-Chimiste/LLMFactory.git@1eb5d0d6b3107e2267552f40ba32810b63859d1b
anthropic[bedrock]
openai
ollama
soundfile
google-generativeai

# Data Processing & Management
pandas
json_repair
pydantic
tqdm

# PDF & Document Processing
docling

# Web & API
requests

# Development & Utilities
python-dotenv
tenacity
markdown2
Jinja2

# Media Processing
pydub  # Audio
moviepy  # Video
pygame  # Visualization

# YouTube Integration
google-api-python-client
google-auth-oauthlib

# WebAPI / DB
fastapi
uvicorn[standard]
websockets
python-multipart
psycopg[binary,pool]
asyncpg

# System monitoring
psutil>=5.9.0

# Credential encryption
cryptography
